<!-- {{ }} を自アプリの値に置換。(例)テーブルは削除。 -->
<!-- 正は alembic/versions のマイグレーション。本書は意図の説明に徹する。 -->
<!-- 監査カラムは全テーブル共通のため下の共通節にまとめ、各表では省略する。 -->

# テーブル定義

物理テーブルの定義と設計意図を記す。DB は PostgreSQL、スキーマ変更は Alembic マイグレーションを正とする。

## 正の所在

- テーブルの正は `alembic/versions/` のマイグレーションファイル。定義変更はマイグレーションで行い、本書は追随する。
- モデル定義は `app/models/models.py`。マスタは `m_`、トランザクションは `t_` プレフィックス。
- 外部認証基盤（ap-safedx）参照テーブルは `ExternalBase` 継承で Alembic 対象外（本アプリからスキーマ変更しない）。

## 共通監査カラム（TimestampMixin）

<!-- 下記は Base + TimestampMixin 継承の全テーブルに付与。各表定義では省略する。 -->

| カラム名 | 型 | 制約 | デフォルト | 説明 |
|----------|-----|------|-----------|------|
| deleted | Boolean | NOT NULL, INDEX | False | 論理削除フラグ |
| created_at | DateTime | NOT NULL, INDEX | CURRENT_TIMESTAMP | 作成日時 |
| created_by | String(254) | NOT NULL | - | 作成者 |
| updated_at | DateTime | NOT NULL | CURRENT_TIMESTAMP（更新時自動） | 更新日時 |
| updated_by | String(254) | NOT NULL | - | 更新者 |
| deleted_at | DateTime | NULL | - | 削除日時 |
| deleted_by | String(254) | NULL | - | 削除者 |

## テーブル定義雛形

<!-- テーブルごとに複製。監査カラムは記載不要。 -->

### {{テーブル名}}（{{論理名}}）

| カラム名 | 型 | 制約 | デフォルト | 説明 |
|----------|-----|------|-----------|------|
| id | {{型}} | PK | {{既定値}} | ID |
| {{カラム}} | {{型}} | {{制約}} | {{既定値}} | {{説明}} |

## （例）m_example_categories（カテゴリマスタ）

| カラム名 | 型 | 制約 | デフォルト | 説明 |
|----------|-----|------|-----------|------|
| id | UUID | PK | uuid4 | ID |
| name | String(100) | NOT NULL, UNIQUE | - | カテゴリ名 |
| sort_order | Integer | NOT NULL | 0 | 並び順 |
| is_active | Boolean | NOT NULL, INDEX | True | 有効フラグ |

## （例）t_example_items（アイテム）

| カラム名 | 型 | 制約 | デフォルト | 説明 |
|----------|-----|------|-----------|------|
| id | UUID | PK | uuid4 | ID |
| name | String(100) | NOT NULL | - | アイテム名 |
| description | Text | NULL | - | 説明 |
| category_id | UUID | NOT NULL, FK(m_example_categories.id), INDEX | - | カテゴリID |
| is_active | Boolean | NOT NULL, INDEX | True | 有効フラグ |

## インデックス設計

<!-- PK・UNIQUE 以外の索引を列挙。設定理由（絞り込み・整合性）を必ず書く。 -->

| テーブル | インデックス対象 | 種別 | 設定理由 |
|----------|-----------------|------|----------|
| (例) t_example_items | category_id | 通常 | カテゴリ絞り込み検索の高速化 |
| (例) 全テーブル | deleted | 通常 | 論理削除除外での一覧取得を高速化 |
| {{テーブル}} | {{カラム}} | {{種別}} | {{理由}} |
