# 認可設計書

<!-- ロールと権限マトリクスを定義する。実装方針は本書末尾の節に記載。 -->

{{アプリ名}} のロールと、機能ごとの操作可否を定義する。

## ロール一覧

| ロール | コード | 説明 | 想定ユーザー |
|---|---|---|---|
| （例）ゲスト | `guest` | 未認証。閲覧不可 | 未ログイン |
| （例）一般 | `general` | 業務データの参照・登録・更新 | 一般利用者 |
| （例）管理者 | `admin` | 一般の権限に加え、マスタ管理・全データ操作 | 管理担当 |
| {{ロール}} | {{コード}} | {{説明}} | {{想定ユーザー}} |

## 権限マトリクス

機能 × ロールで操作可否を示す。◯=可、△=条件付き、×=不可。

| 機能 / 操作 | ゲスト | 一般 | 管理者 |
|---|---|---|---|
| （例）ログイン | ◯ | ◯ | ◯ |
| （例）アイテム参照 | × | ◯ | ◯ |
| （例）アイテム作成・更新 | × | ◯ | ◯ |
| （例）アイテム削除 | × | ◯ | ◯ |
| （例）カテゴリ（マスタ）管理 | × | × | ◯ |
| {{機能 / 操作}} | {{可否}} | {{可否}} | {{可否}} |

<!-- △（条件付き）を使う場合は下に条件を明記する。 -->

- {{条件付き可否の条件。例「一般は自分が作成したデータのみ更新可」。無ければ削除}}

## 権限チェックの実装方針

- 認証は `get_current_user_or_redirect`（`app/core/dependencies.py`）で全エンドポイント共通に確認する。
- 認可は FastAPI の依存性注入でチェックする。ロール判定をルーターの `Depends` に組み込み、権限不足時は 403 を返す。
- チェックはサーバー側で必ず行う。画面側の表示制御は補助であり、権限判定の正としない。
- 所有者チェック等の複雑な判定を追加する場合は 03_詳細設計/01_処理設計.md に記載し、本マトリクスと対応させる。参考実装は `app/features/example/`。
