# PoC・トライアル記録: (対象の名前)

> ファイル名は `NNNN-対象名.md`。1検証 = 1ファイル。試行コードは本番に持ち込まない。

## 仮説

> 「〜を使えば〜できるはず」を1文で書く。

## 判定基準

> 採用 / 不採用を数字で判定できる基準を先に決める。例: 「帳票1枚の生成が3秒以内」。

## 期限

> 検証を打ち切る日。延長するなら理由を追記する。

## 結果

> 実測値・確認できた事実だけを書く。

## 採否と理由

> 採用 / 不採用 / 条件付き採用。不採用の理由は将来の自分への申し送りとして具体的に書く。

<details>
<summary>記入例</summary>

対象: 帳票PDF生成ライブラリの比較

- 仮説: WeasyPrint を使えば、既存のHTMLテンプレートから月次帳票PDFを生成できるはず。
- 判定基準: 帳票1枚の生成が3秒以内。日本語フォントの埋め込みが崩れない。
- 期限: 2026-07-18
- 結果: 生成時間は平均1.2秒(50件で計測)。罫線を含む表の改ページで2件レイアウト崩れ。
- 採否と理由: 条件付き採用。改ページ位置を制御するCSSを規約化すれば実用になる。ReportLab 案は座標指定の保守が属人化するため不採用。

</details>
