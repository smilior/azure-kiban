# テスト計画書(テンプレート)

案件未定の共通ひな形である。プロジェクト開始時に各見出しの引用部分を確定させ、
削除してから使用する。

## 目次

1. [テストの出どころと検収](#テストの出どころと検収)
2. [レベル別配分](#レベル別配分)
3. [テスト観点表](#テスト観点表)
4. [テスト用DB方針](#テスト用db方針)
5. [UAT(利用部門の受入確認)](#uat利用部門の受入確認)

## テストの出どころと検収

> AIにテストを出力させるときの正本(入力文書)と、人間が検収する「母数」の定義。数が合っているかだけを人間が見る。

| レベル | 正本(入力文書) | 母数(あるべき本数) | 出力のさせ方 |
|---|---|---|---|
| 総合 | 02_requirements.md の受け入れ条件 | シナリオ数(US-XX-Sn の総数) | ストーリー着手時に一括(旗を立てる) |
| 結合 | 04_architecture.md のAPI仕様(OpenAPI)・ER図・マイグレーション定義(+02の権限マトリクス・同時利用者数) | エンドポイント数×代表ケース+スキーマ制約数 | 基本設計の確定後に一括 |
| 単体 | 対になる文書はない(CLAUDE.md の規約の下で書く) | 事前に決めない | 内側ループで1本ずつ。一覧を事前生成しない |

**二段階出力の手順**(総合・結合の一括生成時):

1. AIに「正本からテストケースの一覧表(ID・シナリオ・観点・レベル)を出して」と依頼する
2. 人間は表の**数と抜けだけ**を検収する(母数と突き合わせる。中身のレビューはここではしない)
3. 承認した表を渡して「このとおりコード化して」と依頼し、全件 Red を確認する

**網羅の突き合わせ**(ストーリー完了時とリリース前): 「02の全シナリオと tests/ を突き合わせ、テストのないシナリオを列挙して」をAIに実行させる。テスト名のシナリオ番号(US-XX-Sn)の欠番が未実装である。

## レベル別配分

> 案件名・実行環境を記入し、比率と実行タイミングをチームで合意する。

| レベル | 比率 | 実行タイミング | 実行環境 | 対象 |
|---|---|---|---|---|
| 単体テスト | 7割 | 毎コミット | ローカル/CI(秒単位) | 関数・クラス単位のロジック |
| 結合テスト | 2割 | 毎PR | CI(本番同一DBコンテナ) | API・DB・外部サービス連携 |
| 総合テスト | 1割 | ストーリー完了時・リリース前 | ステージング環境 | ユーザーシナリオ全体 |

## テスト観点表

> 機能一覧からストーリー単位で行を追加する。境界値・異常系・権限の3列は空欄のまま残さない。

| ID | 対象機能 | 観点 | 境界値 | 異常系 | 権限 | 期待結果 | 実施レベル |
|---|---|---|---|---|---|---|---|
| [ID] | [機能名] | [確認する挙動] | [上限・下限・0・空] | [例外時の応答] | [ロール別の可否] | [期待する結果] | [単体/結合/総合] |

<details>
<summary>記入例(結合テスト・予約登録API)</summary>

| ID | 対象機能 | 観点 | 境界値 | 異常系 | 権限 | 期待結果 | 実施レベル |
|---|---|---|---|---|---|---|---|
| T-001 | 予約登録API | 定員超過時の登録可否 | 残席0・残席1・残席-1(不整合データ) | DB接続断時にリトライせず即座に500を返す | 管理者は定員上限を上書き可能、一般ユーザーは不可 | 残席0での登録は409を返し、DBに書き込まない | 結合 |

</details>

## テスト用DB方針

> 案件のDB製品名・バージョンを記入し、単体テストのフェイク実装と本番スキーマの乖離をレビューで定期確認する。

| レベル | DB方針 | データ | 理由 |
|---|---|---|---|
| 単体テスト | フェイク(インメモリ実装) | テスト内に直書き | 実DB接続を持たず、秒単位で実行するため |
| 結合テスト | 本番と同一のDB製品・同一バージョンをコンテナで起動 | テストごとに投入・分離(分離・クリーンアップ方針の正本は 04_architecture.md「DB定義・マイグレーション」) | SQLite代用は本番との挙動差異を見逃す |
| 総合テスト | 本番と同一構成のステージング環境 | 個人情報マスキング済みの本番相当データ | 本番相当のデータ量・制約で検証するため |

## UAT(利用部門の受入確認)

> 利用部門の担当者名・確認期限・差し戻しフローを確定してから記入する。

1. ストーリー完了時に受け入れ条件(Given/When/Then)から総合テストシナリオを作成し、
   利用部門に共有する。
2. 利用部門の担当者がステージング環境でシナリオを実行し、結果を記録する。
3. NG項目は起票し、修正後に同一シナリオで再確認する。
4. リリース前に全シナリオの再確認済み記録を確認し、承認者の署名を残す。
